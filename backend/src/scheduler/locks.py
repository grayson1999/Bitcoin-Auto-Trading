"""
스케줄러 작업 동시성 제어

포지션을 변경하는 여러 작업(익절, 신호 매매, 복구, 동기화)이 서로 다른
세션으로 동일 Position을 병행 처리해 보유량 초과 매도 등이 발생하는 것을
막기 위한 공유 락.

단일 프로세스 APScheduler(동일 이벤트 루프)에서 동작하므로 asyncio.Lock으로
충분하다. 모든 포지션 변경 작업은 이 락을 획득한 뒤 실행한다.
"""

import asyncio

# 포지션 변경 작업 직렬화 락 (모듈 전역 단일 인스턴스)
position_lock = asyncio.Lock()
